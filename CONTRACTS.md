# Reeled In — Shared Contracts (`CONTRACTS.md`)

**This file is law.** It is the single source of truth for every name, data shape, entity, route, enum, and constant that crosses between people. If two docs disagree, `CONTRACTS.md` wins. Do not rename a field, route, collection, or enum value in your own code — change it *here first*, mark it, tell the team, then update code. The whole point: A, B, C, and D build in parallel and everything snaps together at integration with zero renaming.

Last updated: 2026-07-18.

## 0. Precedence & change process
- Precedence: **`CONTRACTS.md` > `TECH_ARCHITECTURE.md` > `PARALLEL_IMPLEMENTATION_PLAN.md` > everything else.** (This file is the authoritative route/field list; the arch doc's endpoints were a rough sketch — these are exact.)
- To change anything here: edit this file first, add a line under §10 Change log (date + what changed), announce in team chat. Never rename unilaterally.

## 1. Global naming rules (non-negotiable)
- **snake_case everywhere** — JSON on the wire, Mongo fields, Python, and the keys the Base44 frontend reads. **No camelCase, no translation layer.** A `test_id` is `test_id` in the DB, in the API JSON, and in the frontend — never `testId`.
- **IDs are prefixed:** `usr_`, `test_`, `var_`, `score_` + a 12-char lowercase hex. e.g. `test_9f2a10bc4d3e`. Generated by the backend (C) on create. Never a bare `id`.
- **The identifier for a test is `test_id` everywhere** — never `id`, `testId`, `run_id`, or `batch_id`.
- **Timestamps:** ISO 8601 UTC strings in fields `created_at` / `updated_at`. e.g. `"2026-07-19T04:20:00Z"`.
- **Enums** are lowercase snake_case strings (see §2). **Booleans** read as `is_*` / `has_*`.
- **Scores are numbers, not strings.** All engagement values and metrics are floats in **[0.0, 1.0]** unless stated otherwise.

## 2. Canonical vocabulary (exact spellings — copy/paste these)
- **The 5 networks** (fixed order, exact keys): `visual`, `auditory`, `language`, `motion`, `default_mode`.
  - Never abbreviate — `dmn`, `defaultMode`, `aud` are all WRONG.
- **The 4 metrics:** `peak`, `sustained`, `retention`, `overall`. Each a float [0,1].
- **Test types:** `upload`, `voice`.
- **Test status:** `pending`, `scoring`, `complete`, `failed`.
- **Objective** (which metric decides the winner): one of the metric keys; default `retention`.
- **Constants:** `SAMPLE_RATE_HZ = 1`, `DB_NAME = "reeled_in"`, `API_BASE = "/api"`. `NETWORKS` and `METRICS` are the ordered lists above.

## 3. The Score Object (the most important shape)
The canonical payload **B produces, C stores, A renders.** Exact shape:
```json
{
  "variant_id": "var_1a2b3c4d5e6f",
  "networks": {
    "visual":       [0.0, 0.12, 0.34],
    "auditory":     [0.0, 0.05, 0.20],
    "language":     [0.0, 0.00, 0.10],
    "motion":       [0.0, 0.22, 0.41],
    "default_mode": [0.0, 0.08, 0.15]
  },
  "metrics": { "peak": 0.82, "sustained": 0.61, "retention": 0.74, "overall": 0.70 },
  "duration_sec": 18.0,
  "sample_rate_hz": 1
}
```
Rules:
- Every `networks.<name>` array has the **same length** = `round(duration_sec * sample_rate_hz)`, aligned to t=0 (video start), one sample per second.
- All array values and all metrics are floats in **[0,1]**, higher = more engagement. Already normalized by B — **A does not re-normalize.**
- `metrics.overall` = B's composite (documented in `scoring/metrics.py`); `retention` = engagement sustained through the final third (the CTA window).
- Missing/failed score → the variant's score is `null` and the test carries `status: "failed"`; A must handle null.

## 4. Entities / MongoDB collections
DB = `reeled_in`. Collections are **plural snake_case**. `_id` holds the prefixed string ID.

**`users`**
```json
{ "_id": "usr_...", "email": "a@b.com", "display_name": "Sunny", "created_at": "..." }
```
**`tests`**
```json
{ "_id": "test_...", "user_id": "usr_...", "type": "upload",
  "objective": "retention", "status": "pending",
  "variant_ids": ["var_...", "var_..."], "winner_variant_id": null,
  "created_at": "...", "updated_at": "..." }
```
**`variants`**
```json
{ "_id": "var_...", "test_id": "test_...", "label": "A",
  "media_key": "media/var_....mp4",
  "params": { "voice_id": "...", "music": "...", "note": "" },
  "created_at": "..." }
```
**`scores`** (one per variant; mirrors the Score Object)
```json
{ "_id": "score_...", "variant_id": "var_...", "test_id": "test_...",
  "networks": { }, "metrics": { },
  "duration_sec": 18.0, "sample_rate_hz": 1, "created_at": "..." }
```
`label` = the human tag shown in the UI ("A"/"B" or "Hook v2"). `params` = a free-form bag of the knobs that define the variant (voice, music, etc.).

## 5. API contract
Base path `API_BASE = /api`. All bodies + responses are JSON, snake_case. All authed (§6). Routes are **locked** once A codes against them.

| Method | Path | Body | Returns |
|---|---|---|---|
| POST | `/api/tests` | `{ type, objective? }` | `Test` |
| POST | `/api/tests/{test_id}/variants` | multipart: `file`, `label`, `params?` | `Variant` |
| POST | `/api/tests/{test_id}/voice-variants` | `{ base_media_key, script, voices? }` | `{ variants: [Variant] }` |
| POST | `/api/tests/{test_id}/score` | — | `Test` (with `status`, `winner_variant_id`) |
| GET  | `/api/tests/{test_id}` | — | `{ test: Test, variants: [Variant], scores: [ScoreObject] }` |
| GET  | `/api/history` | — | `{ tests: [Test] }` |
| POST | `/api/suggest` (opt) | `{ context }` | `{ suggestions: [string] }` |
| GET  | `/api/tests/{test_id}/tips` (opt) | — | `{ tips: string }` |

- `Test`, `Variant`, `ScoreObject` are exactly the shapes in §3–§4.
- The winner is decided by the test's `objective`; the backend sets `winner_variant_id` at score time.

## 6. Auth
- Every request carries `Authorization: Bearer <token>` (Base44 issues it at login).
- The backend (C) resolves the token → `user_id` (`usr_...`) and, on first login, upserts a `users` doc.
- The contract we rely on: an authed request always yields a stable `user_id`. We adapt to Base44's actual token format; nothing else in the app parses the token.

## 7. Media & storage
- Video files are referenced by an opaque **`media_key`** string, format `media/<variant_id>.mp4`.
- Storage backend is C's concern (Modal volume / temp bucket); everyone else passes the `media_key`, never a raw path or URL.

## 8. Env vars / secret names (exact — everyone uses these names)
```
MONGODB_URI            # Atlas connection string
MONGODB_DB=reeled_in
HF_TOKEN               # HuggingFace, for gated LLaMA-3.2
ELEVENLABS_API_KEY
BACKBOARD_API_KEY
GEMINI_API_KEY         # optional
MODAL_TOKEN_ID
MODAL_TOKEN_SECRET
BACKEND_API_KEY        # shared secret Base44's courier uses to call our API
```
Real values live in `.env` (gitignored) / Modal & Base44 secret stores. Never commit secrets.

## 9. Error format
Non-2xx responses return:
```json
{ "error": { "code": "not_found", "message": "test_id does not exist" } }
```
Common codes: `bad_request`, `unauthorized`, `not_found`, `scoring_failed`, `internal`. HTTP status set accordingly.

## 10. Change log
- 2026-07-18 — initial contract drafted (A/B/C/D to ratify in Phase 0 before freezing).
